# Copy to .env and fill in when we wire up the agents.
# Qwen / DashScope (OpenAI-compatible mode) - not needed for the hello-world boilerplate.
QWEN_API_KEY=
QWEN_MODEL=qwen-plus
QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# Server
HOST=127.0.0.1
PORT=8000
